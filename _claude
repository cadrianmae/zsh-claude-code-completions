#compdef claude

# zsh completion for the Claude Code CLI.
#
# Generated by bin/gen-completion from claude 2.1.226 --help.
# Do not edit by hand; edit overrides.json or helpers.zsh and
# run `just generate`.

# Dynamic completion helpers, inlined into the generated _claude.
#
# These read the user's own Claude Code state, so they cannot be derived from
# --help. Each returns non-zero when the state it reads is absent, which is
# the normal case on a fresh install, and zsh then falls back to no candidates
# rather than showing an error.

__claude_agents() {
  local -a agents
  local dir file

  for dir in "$PWD/.claude/agents" "$HOME/.claude/agents"; do
    [[ -d $dir ]] || continue
    for file in $dir/*.md(N); do
      agents+=("${file:t:r}")
    done
  done

  (( $#agents )) || return 1
  _describe -t agents 'agent' agents
}

__claude_sessions() {
  # Transcripts are stored per project directory, with the path slugified:
  # every '/' becomes '-', including the leading one.
  local dir="$HOME/.claude/projects/${PWD//\//-}"
  local -a sessions
  local file

  [[ -d $dir ]] || return 1
  # (Nom) sorts newest first, so the session you just left leads the list.
  for file in $dir/*.jsonl(Nom); do
    sessions+=("${file:t:r}")
  done

  (( $#sessions )) || return 1
  _describe -t sessions 'session' sessions
}

__claude_marketplaces() {
  local dir="$HOME/.claude/plugins/marketplaces"
  local -a marketplaces
  local entry

  [[ -d $dir ]] || return 1
  for entry in $dir/*(N/); do
    marketplaces+=("${entry:t}")
  done

  (( $#marketplaces )) || return 1
  _describe -t marketplaces 'marketplace' marketplaces
}

__claude_plugins() {
  local config="$HOME/.claude/plugins/installed_plugins.json"
  local -a plugins

  [[ -r $config ]] || return 1
  (( $+commands[jq] )) || return 1
  plugins=(${(f)"$(jq -r '.plugins? // {} | keys[]' -- $config 2>/dev/null)"})

  (( $#plugins )) || return 1
  _describe -t plugins 'plugin' plugins
}

_claude_commands() {
  local -a commands
  commands=(
    'agents:Manage background agents'
    'auth:Manage authentication'
    'auto-mode:Inspect or reset auto mode classifier configuration'
    'doctor:Check the health of your Claude Code installation'
    'gateway:Run the enterprise auth/telemetry gateway'
    'import:Import config from another AI coding agent into Claude Code'
    'install:Install Claude Code native build'
    'mcp:Configure and manage MCP servers'
    'plugin:Manage Claude Code plugins'
    'plugins:Manage Claude Code plugins'
    'project:Manage Claude Code project state'
    'setup-token:Set up a long-lived authentication token (requires Claude subscription)'
    'ultrareview:Run a cloud-hosted multi-agent code review of the current branch (or a PR number / base br'
    'update:Check for updates and install if available'
    'upgrade:Check for updates and install if available'
  )
  _describe -t commands 'claude command' commands
}

_claude() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '*--add-dir[Additional directories to allow tool access to]:directories:_files -/' \
    '--agent[Agent for the current session. Overrides the '\''agent'\'' setting]:agent:__claude_agents' \
    '--agents[JSON object defining custom agents (e.g]:json: ' \
    '--allow-dangerously-skip-permissions[Enable bypassing all permission checks as an option, without it being enabled by default]' \
    '*--allowedTools[Comma or space-separated list of tool names to allow (e.g. "Bash(git *) Edit")]:tools: ' \
    '*--allowed-tools[Comma or space-separated list of tool names to allow (e.g. "Bash(git *) Edit")]:tools: ' \
    '--append-system-prompt[Append a system prompt to the default system prompt]:prompt: ' \
    '--autocompact[Auto-compact window size (auto, or 100k–1M tokens)]:auto|tokens: ' \
    '--ax-screen-reader[Render screen-reader friendly output (flat text, no decorative borders or animations)]' \
    '(--bg --background)--bg[Start the session as a background agent and return immediately (manage with `claude agents]' \
    '(--bg --background)--background[Start the session as a background agent and return immediately (manage with `claude agents]' \
    '--bare[Minimal mode\: skip hooks, LSP, plugin sync, attribution, auto-memory, background prefetche]' \
    '*--betas[Beta headers to include in API requests (API key users only)]:betas: ' \
    '--brief[Enable SendUserMessage tool for agent-to-user communication]' \
    '--chrome[Enable Claude in Chrome integration]' \
    '--cloud[Create a cloud session with the given description, or attach to an existing one by session]::description|session_id|url: ' \
    '(-c --continue)-c[Continue the most recent conversation in the current directory]' \
    '(-c --continue)--continue[Continue the most recent conversation in the current directory]' \
    '--dangerously-skip-permissions[Bypass all permission checks. Recommended only for sandboxes with no internet access]' \
    '(-d --debug)-d[Enable debug mode with optional category filtering (e.g., "api,hooks" or "!1p,!file")]::filter: ' \
    '(-d --debug)--debug[Enable debug mode with optional category filtering (e.g., "api,hooks" or "!1p,!file")]::filter: ' \
    '--debug-file[Write debug logs to a specific file path (implicitly enables debug mode)]:path:_files' \
    '--disable-slash-commands[Disable all skills]' \
    '*--disallowedTools[Comma or space-separated list of tool names to deny (e.g. "Bash(git *) Edit")]:tools: ' \
    '*--disallowed-tools[Comma or space-separated list of tool names to deny (e.g. "Bash(git *) Edit")]:tools: ' \
    '--effort[Effort level for the current session (low, medium, high, xhigh, max)]:level:(low medium high xhigh max)' \
    '--environment[Create a new cloud session that runs on the given self-hosted environment (ccpool_...)]:environment_id: ' \
    '--exclude-dynamic-system-prompt-sections[Move per-machine sections (cwd, env info, memory paths, git status) from the system prompt]' \
    '--fallback-model[Enable automatic fallback to specified model(s) when the default model is overloaded or no]:model:(fable opus sonnet haiku)' \
    '*--file[File resources to download at startup]:specs: ' \
    '--fork-session[When resuming, create a new session ID instead of reusing the original (use with --resume]' \
    '--forward-subagent-text[Forward subagent text and thinking blocks as assistant/user messages with parent_tool_use_]' \
    '--from-pr[Resume a session linked to a PR by PR number/URL, or open interactive picker with optional]::value: ' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--ide[Automatically connect to IDE on startup if exactly one valid IDE is available]' \
    '--include-hook-events[Include all hook lifecycle events in the output stream (only works with --output-format=st]' \
    '--include-partial-messages[Include partial message chunks as they arrive (only works with --print and --output-format]' \
    '--input-format[Input format (only works with --print)\: "text" (default), or "stream-json" (realtime strea]:format:(text stream-json)' \
    '--json-schema[JSON Schema for structured output validation]:schema:_files' \
    '--max-budget-usd[Maximum dollar amount to spend on API calls (only works with --print)]:amount: ' \
    '*--mcp-config[Load MCP servers from JSON files or strings (space-separated)]:configs: ' \
    '--model[Model for the current session]:model:(fable opus sonnet haiku claude-fable-5 claude-opus-5 claude-sonnet-5 claude-haiku-4-5-20251001)' \
    '(-n --name)-n[Set a display name for this session (shown in the prompt box, /resume picker, and terminal]:name: ' \
    '(-n --name)--name[Set a display name for this session (shown in the prompt box, /resume picker, and terminal]:name: ' \
    '--no-chrome[Disable Claude in Chrome integration]' \
    '--no-session-persistence[Disable session persistence - sessions will not be saved to disk and cannot be resumed (on]' \
    '--output-format[Output format (only works with --print)\: "text" (default), "json" (single result), or "str]:format:(text json stream-json)' \
    '--permission-mode[Permission mode to use for the session]:mode:(acceptEdits auto bypassPermissions manual dontAsk plan)' \
    '--plugin-dir[Load a plugin from a directory or .zip for this session only (repeatable\: --plugin-dir A -]:path:_files' \
    '--plugin-url[Fetch a plugin .zip from a URL for this session only (repeatable\: --plugin-url A --plugin-]:url: ' \
    '(-p --print)-p[Print response and exit (useful for pipes)]' \
    '(-p --print)--print[Print response and exit (useful for pipes)]' \
    '--prompt-suggestions[Enable prompt suggestions]::value:(true false 1 0 yes no on off)' \
    '--remote-control[Start an interactive session with Remote Control enabled (optionally named)]::name: ' \
    '--remote-control-session-name-prefix[Prefix for auto-generated Remote Control session names]:prefix: ' \
    '--replay-user-messages[Re-emit user messages from stdin back on stdout for acknowledgment (only works with --inpu]' \
    '(-r --resume)-r[Resume a conversation by session ID, or open interactive picker with optional search term]::value:__claude_sessions' \
    '(-r --resume)--resume[Resume a conversation by session ID, or open interactive picker with optional search term]::value:__claude_sessions' \
    '--safe-mode[Start with all customizations (CLAUDE.md, skills, plugins, hooks, MCP servers, custom comm]' \
    '--session-id[Use a specific session ID for the conversation (must be a valid UUID)]:uuid: ' \
    '--setting-sources[Comma-separated list of setting sources to load (user, project, local)]:sources:(user project local)' \
    '--settings[Path to a settings JSON file or a JSON string to load additional settings from]:file-or-json:_files' \
    '--strict-mcp-config[Only use MCP servers from --mcp-config, ignoring all other MCP configurations]' \
    '--system-prompt[System prompt to use for the session]:prompt: ' \
    '--teleport[Resume a teleport session, optionally specify session ID]::session: ' \
    '--tmux[Create a tmux session for the worktree (requires --worktree)]' \
    '*--tools[Specify the list of available tools from the built-in set]:tools: ' \
    '--verbose[Override verbose mode setting from config]' \
    '(-v --version)-v[Output the version number]' \
    '(-v --version)--version[Output the version number]' \
    '(-w --worktree)-w[Create a new git worktree for this session (optionally specify a name)]::name: ' \
    '(-w --worktree)--worktree[Create a new git worktree for this session (optionally specify a name)]::name: ' \
    '1: :_claude_commands' \
    '*:: :->subcommand' && ret=0

  case "$state" in
    subcommand)
      case "$words[1]" in
        agents) _claude_agents && ret=0 ;;
        auth) _claude_auth && ret=0 ;;
        auto-mode) _claude_auto_mode && ret=0 ;;
        doctor) _claude_doctor && ret=0 ;;
        gateway) _claude_gateway && ret=0 ;;
        import) _claude_import && ret=0 ;;
        install) _claude_install && ret=0 ;;
        mcp) _claude_mcp && ret=0 ;;
        plugin|plugins) _claude_plugin && ret=0 ;;
        project) _claude_project && ret=0 ;;
        setup-token) _claude_setup_token && ret=0 ;;
        ultrareview) _claude_ultrareview && ret=0 ;;
        update|upgrade) _claude_update && ret=0 ;;
      esac
      ;;
  esac

  return ret
}

_claude_agents() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--add-dir[Additional directory to allow tool access to in dispatched sessions (repeatable)]:directory:_files -/' \
    '--agent[Default agent for sessions dispatched from agent view. Overrides the '\''agent'\'' setting]:agent:__claude_agents' \
    '--all[With --json\: also include completed background sessions]' \
    '--allow-dangerously-skip-permissions[Make bypass-permissions mode available to dispatched sessions without defaulting to it]' \
    '--cwd[Show only background sessions started under <path>]:path:_files' \
    '--dangerously-skip-permissions[Alias for --permission-mode bypassPermissions]' \
    '--effort[Default effort level for sessions dispatched from agent view]:level:(low medium high xhigh max)' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--json[Print active sessions (interactive and background) as a JSON array and exit (for scripting]' \
    '--mcp-config[MCP server configuration to apply to dispatched sessions (repeatable)]:config: ' \
    '--model[Default model for sessions dispatched from agent view]:model:(fable opus sonnet haiku claude-fable-5 claude-opus-5 claude-sonnet-5 claude-haiku-4-5-20251001)' \
    '--permission-mode[Default permission mode for sessions dispatched from agent view]:mode: ' \
    '--plugin-dir[Load plugins from specified directory for the agent view and dispatched sessions (repeatab]:path:_files' \
    '--setting-sources[Comma-separated list of setting sources to load (user, project, local)]:sources:(user project local)' \
    '--settings[Settings file or JSON string to apply to the agent view and dispatched sessions]:file-or-json:_files' \
    '--strict-mcp-config[Only use MCP servers from --mcp-config in dispatched sessions]' && ret=0

  return ret
}

_claude_auth_commands() {
  local -a commands
  commands=(
    'login:Sign in to your Anthropic account'
    'logout:Log out from your Anthropic account'
    'status:Show authentication status'
  )
  _describe -t commands 'auth command' commands
}

_claude_auth() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '1: :_claude_auth_commands' \
    '*:: :->subcommand' && ret=0

  case "$state" in
    subcommand)
      case "$words[1]" in
        login) _claude_auth_login && ret=0 ;;
        logout) _claude_auth_logout && ret=0 ;;
        status) _claude_auth_status && ret=0 ;;
      esac
      ;;
  esac

  return ret
}

_claude_auth_login() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--claudeai[Use Claude subscription (default)]' \
    '--console[Use Anthropic Console (API usage billing) instead of Claude subscription]' \
    '--email[Pre-populate email address on the login page]:email: ' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--sso[Force SSO login flow]' && ret=0

  return ret
}

_claude_auth_logout() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_auth_status() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--json[Output as JSON (default)]' \
    '--text[Output as human-readable text]' && ret=0

  return ret
}

_claude_auto_mode_commands() {
  local -a commands
  commands=(
    'config:Print the effective auto mode config as JSON\: your settings where set, defaults otherwise'
    'critique:Get AI feedback on your custom auto mode rules'
    'defaults:Print the default auto mode environment, allow, soft_deny, and hard_deny rules as JSON'
    'reset:Reset auto mode configuration to the shipped defaults by removing the autoMode section fro'
  )
  _describe -t commands 'auto-mode command' commands
}

_claude_auto_mode() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '1: :_claude_auto_mode_commands' \
    '*:: :->subcommand' && ret=0

  case "$state" in
    subcommand)
      case "$words[1]" in
        config) _claude_auto_mode_config && ret=0 ;;
        critique) _claude_auto_mode_critique && ret=0 ;;
        defaults) _claude_auto_mode_defaults && ret=0 ;;
        reset) _claude_auto_mode_reset && ret=0 ;;
      esac
      ;;
  esac

  return ret
}

_claude_auto_mode_config() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_auto_mode_critique() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--model[Override which model is used]:model:(fable opus sonnet haiku claude-fable-5 claude-opus-5 claude-sonnet-5 claude-haiku-4-5-20251001)' && ret=0

  return ret
}

_claude_auto_mode_defaults() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--label[Show only rules whose label starts with this prefix (case-insensitive)]:prefix: ' && ret=0

  return ret
}

_claude_auto_mode_reset() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-y --yes)-y[Skip the confirmation prompt]' \
    '(-y --yes)--yes[Skip the confirmation prompt]' && ret=0

  return ret
}

_claude_doctor() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_gateway() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--config[Path to gateway YAML config]:path:_files' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_import() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--dry-run[Show what would be imported without writing anything]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--yes[Skip the interactive picker]' && ret=0

  return ret
}

_claude_install() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--force[Force installation even if already installed]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_mcp_commands() {
  local -a commands
  commands=(
    'add:Add an MCP server to Claude Code'
    'add-from-claude-desktop:Import MCP servers from Claude Desktop (Mac and WSL only)'
    'add-json:Add an MCP server (stdio or SSE) with a JSON string'
    'get:Get details about an MCP server'
    'list:List configured MCP servers'
    'login:Authenticate with an MCP server (HTTP, SSE, or claude.ai connector)'
    'logout:Clear stored OAuth credentials for an MCP server'
    'remove:Remove an MCP server'
    'reset-project-choices:Reset all approved and rejected project-scoped (.mcp.json) servers within this project'
    'serve:Start the Claude Code MCP server'
  )
  _describe -t commands 'mcp command' commands
}

_claude_mcp() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '1: :_claude_mcp_commands' \
    '*:: :->subcommand' && ret=0

  case "$state" in
    subcommand)
      case "$words[1]" in
        add) _claude_mcp_add && ret=0 ;;
        add-from-claude-desktop) _claude_mcp_add_from_claude_desktop && ret=0 ;;
        add-json) _claude_mcp_add_json && ret=0 ;;
        get) _claude_mcp_get && ret=0 ;;
        list) _claude_mcp_list && ret=0 ;;
        login) _claude_mcp_login && ret=0 ;;
        logout) _claude_mcp_logout && ret=0 ;;
        remove) _claude_mcp_remove && ret=0 ;;
        reset-project-choices) _claude_mcp_reset_project_choices && ret=0 ;;
        serve) _claude_mcp_serve && ret=0 ;;
      esac
      ;;
  esac

  return ret
}

_claude_mcp_add() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--callback-port[Fixed port for OAuth callback (for servers requiring pre-registered redirect URIs)]:port: ' \
    '--client-id[OAuth client ID for HTTP/SSE servers]:clientId: ' \
    '--client-secret[Prompt for OAuth client secret (or set MCP_CLIENT_SECRET env var)]' \
    '*-e[Set environment variables (e.g. -e KEY=value)]:env: ' \
    '*--env[Set environment variables (e.g. -e KEY=value)]:env: ' \
    '*-H[Set WebSocket headers (e.g. -H "X-Api-Key\: abc123" -H "X-Custom\: value")]:header: ' \
    '*--header[Set WebSocket headers (e.g. -H "X-Api-Key\: abc123" -H "X-Custom\: value")]:header: ' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Configuration scope (local, user, or project)]:scope: ' \
    '(-s --scope)--scope[Configuration scope (local, user, or project)]:scope: ' \
    '(-t --transport)-t[Transport type (stdio, sse, http). Defaults to stdio if not specified]:transport: ' \
    '(-t --transport)--transport[Transport type (stdio, sse, http). Defaults to stdio if not specified]:transport: ' && ret=0

  return ret
}

_claude_mcp_add_from_claude_desktop() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Configuration scope (local, user, or project)]:scope: ' \
    '(-s --scope)--scope[Configuration scope (local, user, or project)]:scope: ' && ret=0

  return ret
}

_claude_mcp_add_json() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--client-secret[Prompt for OAuth client secret (or set MCP_CLIENT_SECRET env var)]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Configuration scope (local, user, or project)]:scope: ' \
    '(-s --scope)--scope[Configuration scope (local, user, or project)]:scope: ' && ret=0

  return ret
}

_claude_mcp_get() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_mcp_list() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_mcp_login() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--no-browser[Print the authorization URL instead of opening a browser (for SSH/headless sessions — past]' && ret=0

  return ret
}

_claude_mcp_logout() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_mcp_remove() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Configuration scope (local, user, or project) - if not specified, removes from whichever s]:scope: ' \
    '(-s --scope)--scope[Configuration scope (local, user, or project) - if not specified, removes from whichever s]:scope: ' && ret=0

  return ret
}

_claude_mcp_reset_project_choices() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_mcp_serve() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-d --debug)-d[Enable debug mode]' \
    '(-d --debug)--debug[Enable debug mode]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--verbose[Override verbose mode setting from config]' && ret=0

  return ret
}

_claude_plugin_commands() {
  local -a commands
  commands=(
    'details:Show a plugin'\''s component inventory and projected token cost'
    'disable:Disable an enabled plugin'
    'enable:Enable a disabled plugin'
    'eval:Run eval cases (evals/**/case.yaml or evals/**/prompt.md + graders/*.md) against a plugin'
    'init:Scaffold a new plugin at ~/.claude/skills/<name>/ (auto-loads next session as <name>@skill'
    'new:Scaffold a new plugin at ~/.claude/skills/<name>/ (auto-loads next session as <name>@skill'
    'install:Install a plugin from available marketplaces (use plugin@marketplace for specific marketpl'
    'i:Install a plugin from available marketplaces (use plugin@marketplace for specific marketpl'
    'list:List installed plugins'
    'marketplace:Manage Claude Code marketplaces'
    'prune:Remove auto-installed dependencies that are no longer needed'
    'autoremove:Remove auto-installed dependencies that are no longer needed'
    'tag:Create a {name}--v{version} git tag for a plugin release, validating that plugin.json and'
    'uninstall:Uninstall an installed plugin'
    'remove:Uninstall an installed plugin'
    'update:Update a plugin to the latest version (restart required to apply)'
    'validate:Validate a plugin or marketplace manifest'
  )
  _describe -t commands 'plugin command' commands
}

_claude_plugin() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '1: :_claude_plugin_commands' \
    '*:: :->subcommand' && ret=0

  case "$state" in
    subcommand)
      case "$words[1]" in
        details) _claude_plugin_details && ret=0 ;;
        disable) _claude_plugin_disable && ret=0 ;;
        enable) _claude_plugin_enable && ret=0 ;;
        eval) _claude_plugin_eval && ret=0 ;;
        init|new) _claude_plugin_init && ret=0 ;;
        install|i) _claude_plugin_install && ret=0 ;;
        list) _claude_plugin_list && ret=0 ;;
        marketplace) _claude_plugin_marketplace && ret=0 ;;
        prune|autoremove) _claude_plugin_prune && ret=0 ;;
        tag) _claude_plugin_tag && ret=0 ;;
        uninstall|remove) _claude_plugin_uninstall && ret=0 ;;
        update) _claude_plugin_update && ret=0 ;;
        validate) _claude_plugin_validate && ret=0 ;;
      esac
      ;;
  esac

  return ret
}

_claude_plugin_details() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '1: :__claude_plugins' && ret=0

  return ret
}

_claude_plugin_disable() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-a --all)-a[Disable all enabled plugins]' \
    '(-a --all)--all[Disable all enabled plugins]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Installation scope\: user, project, local]:scope: ' \
    '(-s --scope)--scope[Installation scope\: user, project, local]:scope: ' \
    '1: :__claude_plugins' && ret=0

  return ret
}

_claude_plugin_enable() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Installation scope\: user, project, local]:scope: ' \
    '(-s --scope)--scope[Installation scope\: user, project, local]:scope: ' \
    '1: :__claude_plugins' && ret=0

  return ret
}

_claude_plugin_eval_commands() {
  local -a commands
  commands=(
    'init:Author an eval suite under evals/ via an interview that sources inputs and designs graders'
  )
  _describe -t commands 'plugin eval command' commands
}

_claude_plugin_eval() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--ablation[Run a no-plugin baseline arm and report the score delta (none | with-without; default\: wit]:mode: ' \
    '*--allow-tools[Operator grant for gated tools (Bash, Write, Edit, WebFetch, mcp__*)]:tools: ' \
    '--case[Filter cases by name glob]:glob: ' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--json[Print the full run result (prompts, graders, per-run scores) as JSON to stdout, or write i]::path:_files' \
    '--judge-model[Override LLM-grader model]:model: ' \
    '--keep-temp[Preserve scaffold dirs for debugging]' \
    '--max-cost-usd[Optional hard cost ceiling; abort and report partial results if hit (exit 2)]:usd: ' \
    '--model[Override model for all cases]:model:(fable opus sonnet haiku claude-fable-5 claude-opus-5 claude-sonnet-5 claude-haiku-4-5-20251001)' \
    '--no-publish[Keep the HTML report local only; skip publishing it to claude.ai]' \
    '--no-scaffold[Explicitly skip scaffold_script]' \
    '--output-dir[Directory for aggregate-result.json]:dir:_files -/' \
    '--publish-report[Also require publishing the report to claude.ai (already the default when your account sup]' \
    '--report[Write the self-contained HTML report (scores, prompts, grader verdicts) to <path> instead]:path:_files' \
    '--runs[Override per-case runs]:n: ' \
    '--scaffold[Run each case'\''s scaffold_script (runs author-supplied bash as you; off by default — only u]' \
    '*--tag[Filter cases by tag (repeatable)]:tag: ' \
    '--threshold[Exit 1 if any case score is below this threshold]:0..1: ' \
    '--verbose[Stream the trace as it runs]' \
    '1: :_claude_plugin_eval_commands' \
    '*:: :->subcommand' && ret=0

  case "$state" in
    subcommand)
      case "$words[1]" in
        init) _claude_plugin_eval_init && ret=0 ;;
      esac
      ;;
  esac

  return ret
}

_claude_plugin_eval_init() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--bare[Write a blank template (prompt.md + graders/criteria.md) instead of running the interview]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-i --interactive)-i[Run the authoring interview (already the default in a terminal); requires an interactive t]' \
    '(-i --interactive)--interactive[Run the authoring interview (already the default in a terminal); requires an interactive t]' && ret=0

  return ret
}

_claude_plugin_init() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--author[Author name]:name: ' \
    '--author-email[Author email]:email: ' \
    '--description[Manifest description]:text: ' \
    '(-f --force)-f[Overwrite an existing .claude-plugin/ at the target]' \
    '(-f --force)--force[Overwrite an existing .claude-plugin/ at the target]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '*--with[Also scaffold\: skills, agents, hooks, mcp, lsp, output-style, channel]:components: ' && ret=0

  return ret
}

_claude_plugin_install() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--config[Set a userConfig option declared in the plugin'\''s manifest (repeatable)]:key=value: ' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Installation scope\: user, project, or local]:scope: ' \
    '(-s --scope)--scope[Installation scope\: user, project, or local]:scope: ' && ret=0

  return ret
}

_claude_plugin_list() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--available[Include available plugins from marketplaces (requires --json)]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--json[Output as JSON]' && ret=0

  return ret
}

_claude_plugin_marketplace_commands() {
  local -a commands
  commands=(
    'add:Add a marketplace from a URL, path, or GitHub repo'
    'list:List all configured marketplaces'
    'remove:Remove a configured marketplace'
    'rm:Remove a configured marketplace'
    'update:Update marketplace(s) from their source - updates all if no name specified'
  )
  _describe -t commands 'plugin marketplace command' commands
}

_claude_plugin_marketplace() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '1: :_claude_plugin_marketplace_commands' \
    '*:: :->subcommand' && ret=0

  case "$state" in
    subcommand)
      case "$words[1]" in
        add) _claude_plugin_marketplace_add && ret=0 ;;
        list) _claude_plugin_marketplace_list && ret=0 ;;
        remove|rm) _claude_plugin_marketplace_remove && ret=0 ;;
        update) _claude_plugin_marketplace_update && ret=0 ;;
      esac
      ;;
  esac

  return ret
}

_claude_plugin_marketplace_add() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--scope[Where to declare the marketplace\: user (default), project, or local]:scope: ' \
    '*--sparse[Limit checkout to specific directories via git sparse-checkout (for monorepos)]:paths:_files' && ret=0

  return ret
}

_claude_plugin_marketplace_list() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--json[Output as JSON]' && ret=0

  return ret
}

_claude_plugin_marketplace_remove() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--scope[Remove the marketplace declaration from a specific settings scope\: user, project, or local]:scope: ' && ret=0

  return ret
}

_claude_plugin_marketplace_update() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_plugin_prune() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--dry-run[List what would be removed without removing]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Prune at scope\: user, project, or local]:scope: ' \
    '(-s --scope)--scope[Prune at scope\: user, project, or local]:scope: ' \
    '(-y --yes)-y[Skip the confirmation prompt (required when stdin or stdout is not a TTY)]' \
    '(-y --yes)--yes[Skip the confirmation prompt (required when stdin or stdout is not a TTY)]' && ret=0

  return ret
}

_claude_plugin_tag() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--dry-run[Print what would be tagged without creating it]' \
    '(-f --force)-f[Skip the dirty-working-tree and tag-already-exists checks]' \
    '(-f --force)--force[Skip the dirty-working-tree and tag-already-exists checks]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-m --message)-m[Tag annotation message (use %s for the version)]:msg: ' \
    '(-m --message)--message[Tag annotation message (use %s for the version)]:msg: ' \
    '--push[Push the tag to --remote after creating it]' \
    '--remote[Remote to push to with --push]:name: ' && ret=0

  return ret
}

_claude_plugin_uninstall() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--keep-data[Preserve the plugin'\''s persistent data directory (~/.claude/plugins/data/{id}/)]' \
    '--prune[Also remove auto-installed dependencies that are no longer needed (requires -y in non-inte]' \
    '(-s --scope)-s[Uninstall from scope\: user, project, or local]:scope: ' \
    '(-s --scope)--scope[Uninstall from scope\: user, project, or local]:scope: ' \
    '(-y --yes)-y[Skip the --prune confirmation prompt (required when stdin or stdout is not a TTY)]' \
    '(-y --yes)--yes[Skip the --prune confirmation prompt (required when stdin or stdout is not a TTY)]' \
    '1: :__claude_plugins' && ret=0

  return ret
}

_claude_plugin_update() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-s --scope)-s[Installation scope\: user, project, local, managed]:scope: ' \
    '(-s --scope)--scope[Installation scope\: user, project, local, managed]:scope: ' \
    '1: :__claude_plugins' && ret=0

  return ret
}

_claude_plugin_validate() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--strict[Treat warnings as errors (exit 1)]' \
    '1: :_files -/' && ret=0

  return ret
}

_claude_project_commands() {
  local -a commands
  commands=(
    'purge:Delete all Claude Code state for a project (transcripts, tasks, file history, config entry'
  )
  _describe -t commands 'project command' commands
}

_claude_project() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '1: :_claude_project_commands' \
    '*:: :->subcommand' && ret=0

  case "$state" in
    subcommand)
      case "$words[1]" in
        purge) _claude_project_purge && ret=0 ;;
      esac
      ;;
  esac

  return ret
}

_claude_project_purge() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '--all[Purge state for every project (mutually exclusive with \[path\])]' \
    '--dry-run[List what would be deleted without deleting anything]' \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '(-i --interactive)-i[Prompt for each item before deleting]' \
    '(-i --interactive)--interactive[Prompt for each item before deleting]' \
    '(-y --yes)-y[Skip confirmation prompt]' \
    '(-y --yes)--yes[Skip confirmation prompt]' && ret=0

  return ret
}

_claude_setup_token() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude_ultrareview() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' \
    '--json[Print the raw bugs.json payload instead of formatted findings]' \
    '--timeout[Maximum minutes to wait for the review to finish]:minutes: ' && ret=0

  return ret
}

_claude_update() {
  local curcontext="$curcontext" state line ret=1
  typeset -A opt_args

  _arguments -C -s -S \
    '(-h --help)-h[Display help for command]' \
    '(-h --help)--help[Display help for command]' && ret=0

  return ret
}

_claude "$@"
